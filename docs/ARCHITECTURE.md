# System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              PITCH PANDA WEBAPP                              │
│                         With Image Analysis Feature                          │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│                              USER INTERFACE                                  │
└─────────────────────────────────────────────────────────────────────────────┘

    ┌───────────────────────────────────────────────────────────────┐
    │                      InputForm.tsx                            │
    │                                                               │
    │  📝 Startup Name Input                                        │
    │  🌐 Website URL Input                                         │
    │  💬 Extra Context Textarea                                    │
    │  🖼️  Image Upload (NEW!)                                      │
    │      ├─ Click to upload button                                │
    │      ├─ Image preview list                                    │
    │      └─ Remove buttons                                        │
    │  🚀 Submit Button                                             │
    └─────────────────────┬─────────────────────────────────────────┘
                          │
                          │ { startup_name, startup_url,
                          │   extra_context, images }
                          ▼
    ┌───────────────────────────────────────────────────────────────┐
    │                        App.tsx                                │
    │                   (Orchestration Layer)                       │
    └───────────────────────────────────────────────────────────────┘
                          │
                          │
        ┌─────────────────┴─────────────────┐
        │                                   │
        │ Are images uploaded?              │
        │                                   │
        ├─ NO ───────────────────────┐     │
        │                             │     │
        └─ YES ─────────────┐         │     │
                            │         │     │
                            ▼         │     │
┌─────────────────────────────────────────────────────────────────────────────┐
│                           VISION AI MODULE (NEW!)                            │
└─────────────────────────────────────────────────────────────────────────────┘

    ┌───────────────────────────────────────────────────────────────┐
    │              src/ai/vision/image-analyzer.ts                  │
    │                                                               │
    │  For each image:                                              │
    │    1. fileToBase64(image) → base64 string                     │
    │    2. analyzeImage(base64) → API call to GPT-4o Vision        │
    │    3. Extract structured data from response                   │
    │                                                               │
    │  Combine all results → formatted markdown text                │
    └─────────────────────┬─────────────────────────────────────────┘
                          │
                          │ imageContext: string
                          │ (structured extracted data)
                          ▼
                    ┌───────────┐
                    │  Merge    │
                    │  Context  │◄──── extra_context (text input)
                    └─────┬─────┘
                          │
                          │ combinedContext =
                          │   textContext + "\n\n" + imageContext
                          │
                          ├───────────────────────────────┐
                          │                               │
                          ▼                               │

┌─────────────────────────────────────────────────────────────────────────────┐
│                    EXISTING LANGGRAPH PIPELINE                               │
│                         (NO CHANGES!)                                        │
└─────────────────────────────────────────────────────────────────────────────┘

    ┌───────────────────────────────────────────────────────────────┐
    │              src/ai/orchestration/graph.ts                    │
    │                                                               │
    │  runAnalysis(startup_name, startup_url, combinedContext)      │
    └───────────────────────┬───────────────────────────────────────┘
                            │
                            ▼
    ┌───────────────────────────────────────────────────────────────┐
    │  Phase 0: Extract Extra Context                               │
    │  ├─ extractExtraContextNode(state)                            │
    │  │   Parses combinedContext (text + images!)                  │
    │  │   Extracts structured ExtraContextData                     │
    │  └─ state.extra_context_parsed                                │
    └─────────────────────┬─────────────────────────────────────────┘
                          │
                          ▼
    ┌───────────────────────────────────────────────────────────────┐
    │  Phase 1: Core Analysis                                       │
    │  ├─ fetchNode() → scrape website                              │
    │  ├─ analyzeNode() → problem/solution analysis                 │
    │  └─ validateNode() → ensure data quality                      │
    └─────────────────────┬─────────────────────────────────────────┘
                          │
                          ▼
    ┌───────────────────────────────────────────────────────────────┐
    │  Phase 2: Extended Analysis (runs in parallel concept)        │
    │  ├─ competitionNode() → find competitors                      │
    │  ├─ teamNode() → analyze team                                 │
    │  ├─ marketNode() → assess market                              │
    │  └─ tractionNode() → evaluate traction & moats                │
    └─────────────────────┬─────────────────────────────────────────┘
                          │
                          │ Analysis object (enriched with image data)
                          ▼
    ┌───────────────────────────────────────────────────────────────┐
    │                    Database Cache                             │
    │  ├─ Save analysis to db.json                                  │
    │  └─ Return analysis to App.tsx                                │
    └─────────────────────┬─────────────────────────────────────────┘
                          │
                          ▼
    ┌───────────────────────────────────────────────────────────────┐
    │                      Result.tsx                               │
    │                   (Display Results)                           │
    │                                                               │
    │  Show complete analysis including:                            │
    │  • Problem & Solution                                         │
    │  • Competition (with image data!)                             │
    │  • Team (with image data!)                                    │
    │  • Market (with image data!)                                  │
    │  • Traction (with image data!)                                │
    └───────────────────────────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────────────────┐
│                          EXTERNAL APIs                                       │
└─────────────────────────────────────────────────────────────────────────────┘

    ┌──────────────────────┐         ┌──────────────────────┐
    │   OpenAI GPT-4o      │         │  OpenAI GPT-4o       │
    │   (Vision)           │         │  (Text Analysis)     │
    │                      │         │                      │
    │  Used by:            │         │  Used by:            │
    │  image-analyzer.ts   │         │  llm.ts              │
    │                      │         │                      │
    │  Model: gpt-4o       │         │  Model: gpt-4o-mini  │
    │  Input: Images       │         │  Input: Text prompts │
    │  Output: Structured  │         │  Output: JSON        │
    │          text        │         │                      │
    └──────────────────────┘         └──────────────────────┘


┌─────────────────────────────────────────────────────────────────────────────┐
│                          DATA FLOW EXAMPLE                                   │
└─────────────────────────────────────────────────────────────────────────────┘

User uploads:
  ✓ pitch-deck-financials.png
  ✓ team-slide.png

Vision AI extracts:
  ### Image 1: pitch-deck-financials.png
  **Financial Data:**
  - MRR: $50K
  - Growth: 15% MoM

  ### Image 2: team-slide.png
  **Team:**
  - CEO: John Smith (ex-Google)
  - CTO: Jane Doe (ex-Amazon)

Combined with text input:
  "Raised $2M seed from Acme Ventures"

Final extra_context:
```

Raised $2M seed from Acme Ventures

---

## DATA EXTRACTED FROM IMAGES:

### Image 1: pitch-deck-financials.png

**Financial Data:**

- MRR: $50K
- Growth: 15% MoM

### Image 2: team-slide.png

**Team:**

- CEO: John Smith (ex-Google)
- CTO: Jane Doe (ex-Amazon)

```

LangGraph processes:
✓ extractExtraContextNode() parses all data
✓ teamNode() enriched with John & Jane's info
✓ tractionNode() enriched with $50K MRR
✓ All nodes receive complete context

Final analysis includes:
• Team: John Smith (CEO, ex-Google), Jane Doe (CTO, ex-Amazon)
• Traction: $50K MRR, 15% MoM growth
• Funding: $2M seed from Acme Ventures


┌─────────────────────────────────────────────────────────────────────────────┐
│                          FILE STRUCTURE                                      │
└─────────────────────────────────────────────────────────────────────────────┘

pitch-panda-webapp/
├── src/
│   ├── ai/
│   │   ├── core/
│   │   │   ├── llm.ts                    (existing - text LLM)
│   │   │   ├── schemas.ts                (existing - types)
│   │   │   └── renderer.ts               (existing)
│   │   ├── orchestration/
│   │   │   └── graph.ts                  (existing - NO CHANGES)
│   │   ├── prompts/
│   │   │   ├── problem_solution.ts       (existing)
│   │   │   ├── competition.ts            (existing)
│   │   │   ├── team.ts                   (existing)
│   │   │   ├── market.ts                 (existing)
│   │   │   ├── traction.ts               (existing)
│   │   │   └── extra_context.ts          (existing)
│   │   └── vision/                       ⭐ NEW MODULE
│   │       ├── image-analyzer.ts         ⭐ NEW
│   │       └── README.md                 ⭐ NEW
│   ├── ui/
│   │   ├── App.tsx                       ✏️ MODIFIED
│   │   ├── InputForm.tsx                 ✏️ MODIFIED
│   │   └── Result.tsx                    (existing - no changes)
│   └── utils/
│       ├── web.ts                        (existing)
│       └── string.ts                     (existing)
└── docs/                                 ⭐ NEW FOLDER
  ├── IMAGE_CONTEXT_FEATURE.md          ⭐ NEW
  ├── IMPLEMENTATION_SUMMARY.md         ⭐ NEW
  └── ARCHITECTURE.md                   ⭐ NEW (this file)


┌─────────────────────────────────────────────────────────────────────────────┐
│                     KEY DESIGN PRINCIPLES                                    │
└─────────────────────────────────────────────────────────────────────────────┘

1. MODULARITY
 ✓ Vision module is completely separate
 ✓ Can be tested independently
 ✓ Can be reused in CLI or other interfaces
 ✓ Zero coupling with main pipeline

2. NON-INVASIVE
 ✓ No changes to LangGraph pipeline
 ✓ No schema modifications
 ✓ Existing functionality untouched
 ✓ Works as preprocessing step

3. CONSISTENCY
 ✓ Image data flows through same path as text
 ✓ Uses existing extra_context mechanism
 ✓ Same validation and processing
 ✓ Unified data model

4. ROBUSTNESS
 ✓ Error handling per image
 ✓ Graceful degradation on failures
 ✓ Sequential processing avoids rate limits
 ✓ Clear error messages

5. TRANSPARENCY
 ✓ Image data clearly marked in context
 ✓ Processing logs to console
 ✓ User sees which images uploaded
 ✓ Loading states show progress

6. MAINTAINABILITY
 ✓ Comprehensive documentation
 ✓ Clean code structure
 ✓ Type safety throughout
 ✓ Easy to extend
```
